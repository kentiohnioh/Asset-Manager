## Packages
recharts | For dashboard analytics charts (bar chart for stock movement)
framer-motion | Smooth page transitions and UI animations
date-fns | Formatting dates for tables and reports
lucide-react | Icons (already in stack, but noting importance)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Outfit", "sans-serif"],
}
